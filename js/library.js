function getInputFildById(id) {
    const inputData = Number(document.getElementById(id).value);
    return inputData;
}
function getTextFildById(id) {
    let TextData = Number(document.getElementById(id).innerText);
    return TextData;
}